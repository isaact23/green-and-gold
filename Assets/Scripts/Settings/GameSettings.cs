namespace GnG.Settings
{
  public class GameSettings
  {
    // Player
    public static readonly int DEFAULT_HEARTS = 8;

    // Inventory
    public static readonly int TOOLBAR_CAPACITY = 8;
    public static readonly int STASH_WIDTH = 8;
    public static readonly int STASH_HEIGHT = 3;
    public static readonly int STASH_CAPACITY = STASH_WIDTH * STASH_HEIGHT;

    // Clusters
    public static readonly int CHUNK_SIZE = 16;
  }
}
